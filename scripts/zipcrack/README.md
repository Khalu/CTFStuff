A very simple ZIP file brute force cracking script using wordlists.


## Useage

``` 
# zipcrack.py --wordlist /usr/share/wordlists/rockyou.txt --zipfile /home/root/passwordzip.zip
```

## Dependencies

Requires zipfile which may need to be installed
